# hand-gesture-recognition
Hand Gesture Recognition using SVM, OpenCV, and the LeapGestRecog dataset. This project classifies different hand gestures from images through preprocessing, feature extraction, model training, and gesture prediction.
